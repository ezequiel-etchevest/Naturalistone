


const invoicesPayments = (simple, repetida) => {
    // let nArray = simple.map(i => {
    //     return  i.payments = repetida.filter(p => p.InvoiceID == i.Naturali_Invoice)
    // })
    // return nArray

}






module.exports = invoicesPayments